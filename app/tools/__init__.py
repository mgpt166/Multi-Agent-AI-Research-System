"""
app/tools/__init__.py
=====================
Search provider abstraction package.

Import the factory function to get the configured provider:
    from app.tools.factory import get_search_provider
    provider = get_search_provider()
    results = provider.search("quantum computing breakthroughs 2025")
"""
